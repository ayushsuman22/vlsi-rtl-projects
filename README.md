# Asynchronous FIFO
Demonstrates independent read/write clocks, Gray-code pointers and synchronizer stages.
Run: `verilator --binary --timing async_fifo.sv tb_async_fifo.sv --top-module tb_async_fifo`
